# Core dependencies
streamlit==1.32.0
pandas==2.2.0
numpy==1.26.3
Pillow==10.2.0

# AI/ML dependencies - stable versions
openai==1.12.0
torch==2.2.0
torchvision==0.17.0
transformers==4.38.0
sentence-transformers==2.5.0

# Vector database
chromadb==0.4.22

# Image processing and computer vision
opencv-python-headless==4.9.0.80
scikit-image==0.22.0
scikit-learn==1.4.0

# CLIP - use pip version instead of git
ftfy==6.1.3
regex==2023.12.25
clip-by-openai==1.0.1

# Data handling
kaggle==1.6.6
python-dotenv==1.0.1
tqdm==4.66.2

# Additional utilities
requests==2.31.0
matplotlib==3.8.3
seaborn==0.13.2
plotly==5.19.0

# Avoid problematic dependencies
# Use headless opencv to avoid GUI issues
# Use specific numpy version to avoid conflicts
# Use stable torch version without CUDA for CPU compatibility
